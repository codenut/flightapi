version:
  1.0

database:
  driverClass: org.postgresql.Driver
  user: ekpkhhbdftavcz
  password: nqtinfS3tsCVJ4Hslw9nP_4KWW
  url: jdbc:postgresql://ec2-204-236-226-63.compute-1.amazonaws.com:5432/d3d6p893vdp30b

server:
  type: simple
  applicationContextPath: /
  #adminContextPath: /admin # If you plan to use an admin path, you'll need to also use non-root app path
  connector:
    type: http
    port: 8080

logging:
  level: INFO
  appenders:
    - type: console
      threshold: ALL
      timeZone: UTC
      target: stdout